node_modules/

# Local-dev MCP config. The plugin declares its MCP server inline in
# .claude-plugin/plugin.json, so this root .mcp.json is a dev-only convenience
# and is NOT part of the shipped plugin.
.mcp.json

# Human-facing docs are not part of the shipped plugin.
docs/

# No build step: the plugin runs from source under Bun. Ignore any stray
# build output if a tool ever emits one.
dist/
*.db
*.db-shm
*.db-wal
*.tsbuildinfo
coverage/
.env
.env.*
.DS_Store

# tooling
.lazy-dev/
.serena/
.idea/
.context-guardian/
.claude/